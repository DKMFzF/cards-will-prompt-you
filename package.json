{
  "name": "cards-will-prompt-you",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "test",
    "format": "prettier --write '**/*.{js,css,json,md}'",
    "check": "prettier --check './**/*.{html,css,js}'",
    "lint": "stylelint './**/*.{css,js}'",
    "lintfix": "stylelint './**/*.{css,js}' --fix",
    "checkAll": "npm run check && npm run lint",
    "fixAll": "npm run format && npm run lintfix",
    "push": "git push origin master"
  },
  "author": "Doroshev Kirill",
  "license": "ISC",
  "description": "none",
  "dependencies": {
    "prettier": "^3.3.3",
    "stylelint": "^16.8.1"
  }
}
